/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"], // Specify the files Tailwind should scan for classes
  theme: {
    extend: {}, // Extend with custom styles if needed
  },
  plugins: [], // Add Tailwind plugins here if needed
};

